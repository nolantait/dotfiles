#!/usr/bin/env bash

# Kills the process running on the provided port
#
# clear-port 3000

if [ -n "$1" ]; then
  echo "Killing processes on port $1..."
  lsof -ti4TCP:"$1" | xargs -r kill
else
  echo >&2 "Usage: clear-port port-number"
  exit 1
fi
